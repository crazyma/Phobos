# === Node.js / Next.js ===
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Next.js build output
.next/
out/
build/
dist/

# Production
/build

# Env files (keep example files tracked)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# === Package managers ===
# Comment out whichever lockfile you're NOT using
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm

# === Testing / coverage ===
coverage/
.nyc_output

# === Drizzle ORM ===
# Local SQLite files if used during dev/testing
*.db
*.sqlite
*.sqlite3
# Drizzle migration metadata is usually committed; uncomment if you regenerate it
# drizzle/

# === PostgreSQL ===
*.sql.gz
*.dump
pgdata/
postgres-data/

# === Tailwind / shadcn/ui ===
# (shadcn/ui components are typically committed; no extra ignores needed)

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.Python
build/
dist/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Python tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/

# Jupyter
.ipynb_checkpoints/

# === Editors / OS ===
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# === Logs ===
logs/
*.log

# === Misc ===
*.tsbuildinfo
.turbo/
.eslintcache

# === Python / uv (etl/) ===
# uv self-ignores .venv via .venv/.gitignore, but be explicit for the repo.
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Local StatsAPI response cache (see etl/src/etl/config.py CACHE_DIR)
etl/.cache/

# === Claude Code ===
# Personal local settings (permissions etc.) — keep .claude/settings.json (if any) tracked
.claude/settings.local.json
# Ephemeral subagent git worktrees
.claude/worktrees/
