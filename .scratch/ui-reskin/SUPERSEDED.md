# ⚠️ 本目錄的 8 票已作廢 — 改看 `.scratch/ui-reskin-v2/issues/`

**日期**：2026-08-12
**取代者**：`.scratch/ui-reskin-v2/issues/01`–`07`

## 為什麼

本目錄的 8 票是**未經 `/to-tickets` skill** 開出來的（依 `docs/agents/issue-tracker.md` 慣例＋仿既有票格式手寫）。事後對照 skill 準則，有兩處實質偏差：

1. **跳過了「Quiz the user」**——沒有先把切法攤給 batu 確認顆粒度與相依邊就直接發布。
2. **`01 設計基礎` 是水平切片**——它明寫「不改任何頁面內容，只換地基」，無法獨立 demo，違反 tracer-bullet 規則。

v2 於 2026-08-12 以 `/to-tickets` 重開，經 batu 確認後發布。主要差異：

- **地基折進第一張頁面票**（v2 的 `01 球員名冊改版（含設計地基）`）——已確認地基變更是純加法、不破壞既有頁面，故不適用 wide-refactor 的 expand–contract 例外，正解就是折進第一個垂直切片。
- 每張票只帶**自己需要的**共用樣板，不預先建一堆沒人用的元件。
- **媒體集錦併入球員頁票**（batu 選擇），8 票 → 7 票。

## 決策內容沒有作廢

兩批票的**技術判斷與決策是一致的**（同一份 `docs/plan/ui-reskin-2026-08-12.md`），差別在切分方式與發布流程。本目錄保留供查閱，**但不要照著做**。
